{
  "name": "new-project3",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "cd simple-fingerprint && bun install && bun run build"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
